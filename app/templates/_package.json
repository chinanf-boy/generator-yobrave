{
	"name": "<%= moduleName %>",
	"version": "0.0.0",
	"description": "<%= moduleDescription %>",
	"license": "MIT",<% if (build) { %>
	"main":"dist/<%= moduleName %>.js",
	"module":"dist/<%= moduleName %>.m.js",<% } %><% if (!build){ %>
	"main":"index.js",	<% } %>
	"repository": "<%= githubUsername %>/<%= repoName %>",
	"author": {
		"name": "<%= name %>",
		"email": "<%= email %>",
		"url": "http://<%= humanizedWebsite %>"
	},<% if (cli) { %>
	"bin": "cli.js",<% } %>
	"engines": {
		"node": ">=4"
	},
	"scripts": {
		"test": "<% if (build) { %>npm run build && <% } %><% if (nyc) { %>nyc <% } %>ava -v",<% if (build) { %>
		"dev": "microbundle watch",
		"build": "microbundle",<% } %>
		"precommit": "lint-staged",
		"pub" : "npm run test && npm run npmUp && npm run git",
		"npmUp": "npm version patch && npm publish",
		"git": "git push && git push --tags"
	},
	"husky": {
		"hooks": {
			"pre-commit": "lint-staged",
		}
	},
	"size-limit": [
		{
		  "path": "dist/<%= moduleName %>.js",
		  "gzip": false
		}
	 ],
	"lint-staged": {
		"linters":{
			"*.{js,json,css,md}": [
				"prettier --single-quote --trailing-comma es5 --write",
				 "git add"
				]
		},
		"ignore":["**/dist/*.js"]
	  },
	"files": [<% if (cli) { %>
		"cli.js",<% } %>
		"src",
		"lib"
	],
	"keywords": [<% if (cli) { %>
		"cli-app",
		"cli",<% } %>
		""
	],
	"dependencies":{
		<% if (cli) { %>
		"commander": "*",
		"turbocolor": "^2.4.5",
		"weoptions": "*",
		"two-log-min": "*"<% } %>
	},
	"devDependencies": {
		<% if (codecov) { %>
		"codecov": "^3.0.2",<% } %><% if (nyc) { %>
		"nyc": "^11.8.0",<% } %>
		"lint-staged": "^7.0.0",<% if (build) { %>
		"microbundle": "^0.4.4",<% } %>
		"husky": "1.0.0-rc.13",
		"ava": "^0.25.0"
	}<% if (codecov) { %>,
	"nyc": {
		"reporter": [
			"lcov",
			"text"
		]
	}<% } %>
}
